{
  "name": "xeokit-integration-examples",
  "private": true,
  "scripts": {
    "dev": "dotenv -- turbo dev --no-daemon",
    "dev-html": "dotenv -- turbo dev --no-daemon --filter=html",
    "dev-vue": "dotenv -- turbo dev --no-daemon --filter=vue",
    "dev-nuxt": "dotenv -- turbo dev --no-daemon --filter=nuxt",
    "build": "dotenv -- turbo build --no-daemon",
    "build-vue": "dotenv -- turbo build --no-daemon --filter=vue",
    "build-nuxt": "dotenv -- turbo build --no-daemon --filter=nuxt",
    "start": "dotenv -- turbo start --no-daemon",
    "start-vue": "dotenv -- turbo start --no-daemon --filter=vue",
    "start-nuxt": "dotenv -- turbo start --no-daemon --filter=nuxt",
    "lint": "turbo lint --no-daemon",
    "clean": "turbo clean --no-daemon"
  },
  "packageManager": "pnpm@8.7.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@turbo/gen": "^1.12.4",
    "cpx": "^1.5.0",
    "dotenv-cli": "^7.2.1",
    "rimraf": "^5.0.5",
    "turbo": "^1.12.4"
  }
}